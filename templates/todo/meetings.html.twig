{% extends 'base.html.twig' %}


{% block body %}
    <div class="container">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-default" ><i class="fa fa-plus" ></i> Add Meeting</button>
    {{ form_end(form) }}
    <div class="container">
{% endblock %}